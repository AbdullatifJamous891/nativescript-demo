<!-- posts-page.xml-->
<Page loaded="pageloaded" navigatingTo="pageNavigatingTo">
    <!-- Each page can have only a single root view -->
    <StackLayout>
        <!-- content here -->
        <Label text="{{email}}"/>
        <Label text="{{name}}"/>
        <Button text="Tap Me!" tap="{{showError}}" class="btn btn-primary btn-active" />
    </StackLayout>
</Page>